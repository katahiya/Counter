<% provide(:title, 'Edit options') %>
<h1>Edit title</h1>

<div class="row">
  <div class="col-md-2"><%= link_to "add options", recorder_add_options_path(@recorder), class: "btn btn-success" %>
</div>

<div class="btn-group btn-group-xs">
  <button type="button" class="btn btn-link dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
    <span class="glyphicon glyphicon-cog" aria-hidden="true"></span>
  </button>
  <ul class="dropdown-menu" role="menu">
    <li><%= link_to "add options", recorder_add_options_path(@recorder) %></li>
  </ul>
</div>

<ul class="recorders">
    <%= render 'options_table' %>
</ul>
<div><%= link_to @recorder.title, recorder_path(@recorder) %>
<div id="edit-option_name" class="modal fade"></div>
