<% provide(:title, 'Edit option')%>
<% provide(:submit_url, option_path(@option)) %>
<% provide(:submit_text, '変更') %>

<%= render "shared/modal_form_template",
           heading: "選択肢: #{@option.name}を変更",
           html: 'edit_form' %>
