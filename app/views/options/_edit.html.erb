<% provide(:title, 'Edit option')%>
<% provide(:submit_url, option_path(@option)) %>
<% provide(:button_text, 'Save changes') %>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <div class="recorder-form">
      <h1>Update <%= @option.name %></h1>
      <%= render 'edit_form' %>
    </div>
  </div>
</div>
