<% if object.errors.any? %>
  $(".error_messages").html("<%= escape_javascript(render 'shared/error_messages', object: object) %>")
<% else %>
  $("<%= rerender_selecter %>").html("<%= escape_javascript(render html_template, html_args) %>")
  $("<%= modal_selecter %>").modal("hide")
<% end %>
