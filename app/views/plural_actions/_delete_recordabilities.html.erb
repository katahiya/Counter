<% provide(:title, 'confirmation')%>
<% provide(:submit_text, '変更') %>

<%= render "delete_commons",
           heading: "記録を削除",
           html: "checked_recordability",
           url: delete_recordabilities_path(ids: params[:ids]) %>

