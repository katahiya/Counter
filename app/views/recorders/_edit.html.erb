<% provide(:title, 'Edit counter')%>
<% provide(:submit_url, recorder_path(@recorder)) %>
<% provide(:button_text, 'Save changes') %>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <div class="recorder-form">
      <h1>Update <%= @recorder.title %></h1>
      <%= render 'title_form' %>
    </div>
  </div>
</div>
