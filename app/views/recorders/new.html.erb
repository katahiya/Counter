<% provide(:title, 'Set up')%>
<% provide(:submit_url, user_setup_path(@user)) %>
<% provide(:button_text, 'Create counter') %>
<h1>Set up</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
  </div>
</div>
