<% provide(:title, 'Add options')%>
<% provide(:submit_url, recorder_add_options_path(@recorder)) %>
<% provide(:button_text, 'Add') %>
<h1>Add options to <%= @recorder.title %></h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'add_options_form' %>
  </div>
</div>

